name: Check and Update HH Resume

on:
  schedule:
    - cron: "*/30 * * * *"  # Проверка каждые 30 минут
  workflow_dispatch:        # Возможность ручного запуска

jobs:
  check-and-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests python-dotenv

      - name: Check last run and update if needed
        env:
          HH_CLIENT_ID: ${{ secrets.HH_CLIENT_ID }}
          HH_CLIENT_SECRET: ${{ secrets.HH_CLIENT_SECRET }}
          HH_REFRESH_TOKEN: ${{ secrets.HH_REFRESH_TOKEN }}
          HH_RESUME_IDS: ${{ secrets.HH_RESUME_IDS }}
          HH_ACCESS_TOKEN: ${{ secrets.HH_ACCESS_TOKEN }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python -c "
          import requests
          import os
          import time
          from datetime import datetime, timedelta
          
          # Функция для отправки сообщений в Telegram
          def send_telegram_message(text):
              token = os.environ.get('TG_BOT_TOKEN')
              chat_id = os.environ.get('TG_CHAT_ID')
              if not token or not chat_id:
                  print('ℹ️ Telegram переменные не заданы — пропускаем отправку.')
                  return
              url = f'https://api.telegram.org/bot{token}/sendMessage'
              payload = {
                  'chat_id': chat_id, 
                  'text': text,
                  'parse_mode': 'HTML'  # Включаем поддержку HTML-форматирования
              }
              try:
                  requests.post(url, data=payload)
              except Exception as e:
                  print(f'⚠️ Ошибка при отправке в Telegram: {e}')
          
          # Получаем информацию о последних workflow runs
          token = os.environ.get('GITHUB_TOKEN')
          repo = os.environ.get('GITHUB_REPOSITORY')
          
          # Ищем историю запусков текущего workflow (check_and_update_resume.yml)
          # Это позволит найти последние успешные обновления
          
          headers = {
              'Authorization': f'token {token}',
              'Accept': 'application/vnd.github.v3+json'
          }
          
          url = f'https://api.github.com/repos/{repo}/actions/runs'
          params = {'per_page': 30}  # Получаем последние 30 запусков
              
          response = requests.get(url, headers=headers, params=params)
          
          if response.status_code != 200:
              print(f'❌ Ошибка при получении истории запусков: {response.status_code}')
              print(response.text)
              exit(1)
          
          # Находим последний успешный запуск с обновлением резюме
          runs = response.json().get('workflow_runs', [])
          # Ищем запуски, в логах которых есть сообщение об успешном обновлении резюме
          successful_runs = [
              run for run in runs 
              if run['conclusion'] == 'success'
          ]
          
          min_interval = timedelta(hours=4, minutes=1)  # 4 часа и 1 минута
          now = datetime.utcnow()
          
          if successful_runs:
              last_success = successful_runs[0]
              last_success_time = datetime.strptime(
                  last_success['updated_at'], '%Y-%m-%dT%H:%M:%SZ'
              )
              
              time_since_last = now - last_success_time
              
              if time_since_last < min_interval:
                  # Еще не прошло достаточно времени
                  elapsed_minutes = time_since_last.total_seconds() / 60
                  remaining_minutes = (min_interval.total_seconds() / 60) - elapsed_minutes
                  
                  msg = f'⏳ Последнее успешное обновление было {elapsed_minutes:.1f} минут назад. '
                  msg += f'Нужно подождать еще {remaining_minutes:.1f} минут.'
                  
                  print(msg)
                  # Не отправляем сообщение в Telegram для экономии уведомлений
                  exit(0)  # Выходим без ошибки, но и без запуска обновления
          
          # Если дошли сюда, значит прошло достаточно времени или нет успешных запусков
          print('✅ Прошло достаточно времени с последнего успешного обновления')
          print('🚀 Запускаем обновление резюме...')
          
          # Уведомляем о старте процесса обновления без даты/времени
          info_msg = '🚀 <b>Запуск обновления резюме</b>'
          send_telegram_message(info_msg)
          
          # Запускаем скрипт напрямую
          import subprocess
          print('🔄 Выполняем скрипт обновления резюме')
          
          try:
              # Запускаем основной скрипт
              result = subprocess.run(['python', 'update_resume.py'], check=True)
              msg = '✅ Скрипт обновления успешно выполнен'
              print(msg)
              # Сообщение отправит сам скрипт update_resume.py
          except subprocess.CalledProcessError as e:
              msg = f'❌ Ошибка при выполнении скрипта: {str(e)}'
              print(f'❌ Ошибка при выполнении скрипта: {str(e)}')
              send_telegram_message(msg)
              exit(1)
          "